# Sandbox: Minu

This template expects a Minu-compatible sandbox HTTP API (`MINU_SANDBOX_URL`).

Registered providers live in `lib/sandbox/factory.ts` — add a new backend by implementing `SandboxProvider` and registering a case there.
